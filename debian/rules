#!/usr/bin/make -f

ifeq ($(USER),buildd)
TEST_NO_NETWORK=1
endif
ifeq ($(USER),pbuilder)
TEST_NO_NETWORK=1
endif

%:
	TEST_NO_NETWORK=$(TEST_NO_NETWORK) dh $@

override_dh_auto_configure:
	cd share && make
	dh_auto_configure
